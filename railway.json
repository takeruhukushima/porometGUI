{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn backend.server:app --host 0.0.0.0 --port $PORT --reload --log-level debug'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/api/health"
  },
  "checks": {
    "enabled": true,
    "path": "/api/health"
  }
}
