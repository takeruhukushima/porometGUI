# Platform-specific dependencies
numpy==1.24.3; sys_platform == 'linux' and python_version >= '3.9'
numpy==1.24.3; sys_platform != 'linux'

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Image processing (lightweight alternatives)
Pillow==10.0.0

# Optional: Only include if absolutely necessary
# scikit-image==0.21.0
# matplotlib==3.7.2
# porespy==2.3.0

# Vercel specific
pycryptodome==3.20.0
